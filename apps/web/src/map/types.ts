/** Tipos del módulo de mapa que se comparten entre SFC (no pueden exportarse desde `<script setup>`). */

/** Modos de definición de zona del analizador (PLAN.md §10.2.4). */
export type DrawMode = 'none' | 'polygon' | 'circle' | 'municipality';
