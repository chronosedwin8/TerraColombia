node_modules/
dist/
.output/
.turbo/
coverage/
*.tsbuildinfo

.env
.env.local
.env.*.local

# Datos descargados y caché de crawler (no versionar volúmenes grandes)
data/
.cache/
data-catalog/**/raw-cache/
storage/
*.gdb/
*.gpkg
*.zip
*.pbf
*.tif

# Salidas de informes
apps/api/reports-out/

# Generado por `pnpm --filter @terracolombia/docs-site glossary` desde packages/shared
apps/docs-site/guia/glosario.md

# Scripts de usar y tirar para inspeccionar la base o la API en desarrollo.
# Se quedan fuera del repositorio: no son herramientas, y cuando alguien corre
# `git add -A` con uno a medias acaban commiteados sin querer.
packages/db/src/cli/tmp*/
packages/db/src/cli/shape/
shapes.json

# Registros de ejecución que dejan las herramientas al vuelo; no son del proyecto.
*.log
